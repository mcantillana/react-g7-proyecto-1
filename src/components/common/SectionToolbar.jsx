import React from 'react';

const SectionToolbar = props => {

    return (
        <div className="col-6">
            
                {props.children}
            
        </div>
    );
}

export default SectionToolbar;